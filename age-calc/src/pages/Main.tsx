import React from "react";
import Form from "../components/Form";

interface Props {}

const Main = (props: Props) => {
  return (
    <main>
      <Form />
      <p>INSERT RESULT</p>
    </main>
  );
};

export default Main;
